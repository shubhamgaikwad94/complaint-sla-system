import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1e3a8a"
    },
    background: {
      default: "#f4f6f8"
    }
  }
});

export default theme;